# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
.coverage
.DS_Store
.vscode
coverage.xml

# Local development artifacts
.env*
!.env.example
local/
.claude/

# Local project presentation artifacts
Mellea-LRC-*.pptx
docs/.obsidian/**

# Evaluation working files: the downloaded dataset and anything a run writes.
# A symlink to ../mellea-lrc-datasets, so no trailing slash: that matches
# directories only and would leave the link itself untracked-but-visible.
data
run-artifact.jsonl
run-validation/
evaluation-result/

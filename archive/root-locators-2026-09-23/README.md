# Archived root-locators implementation

This directory preserves the extraction, validation, provider, evaluation, script, and test code from before the September 2026 rebuild. These modules are outside `src/` and are not part of the active package or test suite. Their imports, scripts, and old README describe the archived implementation and are retained as historical reference.

The active package currently contains only preprocessing and its required model types. Rebuild extraction from its contract and data, rather than importing archived stages back into the package.
